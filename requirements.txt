requests
beautifulsoup4
feedparser
python-dotenv
google-genai
edge-tts
pydub
podgen
pytz
soundfile
numpy

# ── TTS Option 3: Kokoro (free, CPU-friendly, GitHub Actions compatible) ─────
# Note: audioop-lts is NOT needed on Python 3.12 (audioop is built-in).
#       audioop-lts only supports Python >=3.13.
kokoro>=0.9.4
ordered_set
